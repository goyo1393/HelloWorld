import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jgescobar.taskmanager',
  appName: 'EasyTaskManager',
  webDir: 'www'
};

export default config;
